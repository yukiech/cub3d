#include <cub3d.h>

void	pistol_fire(t_vars *vars)
{
	int	i;

	i = 0;
	(void)vars;
/*	while (i <= 200)
	{
		vars->pistol.state = 0;
//			mlx_put_image_to_window(vars->mlx, vars->win, vars->pistol.pistol1.img, 300, 300);
		i++;
	}
	while (i > 200 && i <= 400)
	{
		vars->pistol.state = 1;
//			mlx_put_image_to_window(vars->mlx, vars->win, vars->pistol.pistol2.img, 300, 300);
		i++;
	}
	while (i > 400 && i <= 600)
	{
		vars->pistol.state = 2;
//			mlx_put_image_to_window(vars->mlx, vars->win, vars->pistol.pistol3.img, 300, 300);
		i++;
	}
	while (i > 600 && i <= 800)
	{
		vars->pistol.state = 3;
//			mlx_put_image_to_window(vars->mlx, vars->win, vars->pistol.pistol4.img, 300, 300);
		i++;
	}
	while (i > 800 && i <= 1000)
	{
		vars->pistol.state = 4;
//			mlx_put_image_to_window(vars->mlx, vars->win, vars->pistol.pistol5.img, 300, 300);
		i++;
	}*/
}
